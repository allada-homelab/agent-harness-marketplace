# GENERATED by bin/agents-index.py — do not hand-edit.
# Source of truth is the frontmatter of each SKILL.md / command file.
#
#   harness: omitted means every harness. tags: are for client-side exclusion
#   (e.g. a work machine excludes `homelab`). Regenerate with:
#       bin/agents-index.py --write
entries:
- name: claude-transcript-analyze
  kind: skill
  path: skills/claude-transcript-analyze/SKILL.md
  harness:
  - claude
  tags:
  - observability
  summary: Analyze exported Claude Code transcripts for hook fire rates, CLAUDE.md adherence, and tool-use
    patterns — without ever loading raw transcript prose into context.
- name: claude-transcript-export
  kind: skill
  path: skills/claude-transcript-export/SKILL.md
  harness:
  - claude
  tags:
  - observability
  summary: Export Claude Code transcript JSONL out of the host and dev-container docker volumes to an
    on-device cache for later analysis.
- name: pi-delegate
  kind: skill
  path: skills/pi-delegate/SKILL.md
  harness:
  - claude
  - dsh
  - pi
  tags:
  - delegation
  - homelab
  summary: Delegate a routine task to the pi.dev coding agent (running on the user's self-hosted open-weight
    LLM).
- name: research
  kind: skill
  path: skills/research/SKILL.md
  harness:
  - claude
  - dsh
  - pi
  tags:
  - doctrine
  summary: Run the research-before-acting ladder as an explicit step — scale depth to blast radius, fan
    out parallel read-only agents, and return a scope with a recommendation.
- name: dual-agent-pr-review
  kind: command
  path: commands/dual-agent-pr-review.md
  harness:
  - claude
  tags: []
  summary: Review a GitHub PR with two independent CLI reviewers (Claude Code and Codex) in parallel,
    then reconcile their findings into one evidence-backed consensus set.
