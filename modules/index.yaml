# GENERATED by bin/render-index.py — do not hand-edit.
# Source of truth: each module's package.json / plugin.json and the frontmatter
# of its SKILL.md files. `harness` omitted on a skill means every harness;
# `tags` are for client-side exclusion. Regenerate: bin/render-index.py --write
modules:
- name: claude-transcripts
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  description: Export Claude Code transcripts out of dev containers and analyze hook fire rates, CLAUDE.md
    adherence and tool-use patterns.
  skills:
  - name: claude-transcript-analyze
    path: modules/claude-transcripts/skills/claude-transcript-analyze/SKILL.md
    harness:
    - claude
    tags:
    - observability
    command: false
    summary: Analyze exported Claude Code transcripts for hook fire rates, CLAUDE.md adherence, and tool-use
      patterns — without ever loading raw transcript prose into context.
  - name: claude-transcript-export
    path: modules/claude-transcripts/skills/claude-transcript-export/SKILL.md
    harness:
    - claude
    tags:
    - observability
    command: false
    summary: Export Claude Code transcript JSONL out of the host and dev-container docker volumes to an
      on-device cache for later analysis.
- name: dual-agent-pr-review
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  description: Review a GitHub PR with Claude Code and Codex in parallel, then reconcile their findings
    into one consensus set.
  skills:
  - name: dual-agent-pr-review
    path: modules/dual-agent-pr-review/skills/dual-agent-pr-review/SKILL.md
    harness:
    - claude
    tags: []
    command: true
    summary: Review a GitHub PR with two independent CLI reviewers (Claude Code and Codex) in parallel,
      then reconcile their findings into one evidence-backed consensus set.
- name: get-shit-done
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: Method for getting a complex or fan-out task done by decomposing it and delegating each
    subtask to the cheapest capable model.
  skills:
  - name: get-shit-done
    path: modules/get-shit-done/skills/get-shit-done/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Method for getting a complex or fan-out software task done by orchestrating subagents — decompose
      the task, delegate each subtask to the cheapest capable model (Sonnet for well-scoped work, Opus
      for hard reasoning) under an Opus/Fable orchestrator, run concurrent Sonnet research (web + context7),
      fan out through a checked dynamic workflow, and adversarially verify before done.
- name: get-shit-done-claude
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  description: 'The Claude Code face of get-shit-done: /get-shit-done-claude:run drives the checked Workflow
    fan-out.'
  skills:
  - name: run
    path: modules/get-shit-done-claude/skills/run/SKILL.md
    harness:
    - claude
    tags: []
    command: true
    summary: Get a complex or fan-out task done — decompose it, delegate each subtask to the cheapest
      capable model under an Opus/Fable orchestrator, research concurrently, fan out through a checked
      dynamic workflow, and adversarially verify (Opus) before reporting done.
- name: minimalist-code-review
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: A pragmatic, anti-over-engineering pull-request reviewer with a warm, Socratic voice.
  skills:
  - name: pragmatic-code-review
    path: modules/minimalist-code-review/skills/pragmatic-code-review/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Review a GitHub pull request, diff, or code change in a pragmatic, anti-over-engineering
      style — a Socratic reviewer who pushes hard on whether code needs to exist, prefers determinism
      and reuse over speculative flexibility, watches runtime/query cost, marks nits explicitly, and keeps
      a warm, concise tone.
- name: research
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: Run the research-before-acting ladder as an explicit step and return a scope with a recommendation.
  skills:
  - name: research
    path: modules/research/skills/research/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - doctrine
    command: true
    summary: Run the research-before-acting ladder as an explicit step — scale depth to blast radius,
      fan out parallel read-only agents, and return a scope with a recommendation.
