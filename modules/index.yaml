# GENERATED by bin/render-index.py — do not hand-edit.
# Source of truth: each module's package.json / plugin.json and the frontmatter
# of its SKILL.md files. `harness` omitted on a skill means every harness;
# `tags` are for client-side exclusion. Regenerate: bin/render-index.py --write
modules:
- name: agent-transcripts
  version: 0.6.6
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: Export Claude Code, pi and dsh transcripts into one on-device cache and index them in a
    derived sqlite database.
  skills:
  - name: transcript-export-claude
    path: modules/agent-transcripts/skills/transcript-export-claude/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - observability
    command: false
    summary: Export Claude Code transcript JSONL from the host config directory and dev-container docker
      volumes into the on-device agent-transcripts cache.
  - name: transcript-export-dsh
    path: modules/agent-transcripts/skills/transcript-export-dsh/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - observability
    command: false
    summary: Export dsh (DeepSeek harness) zstd session logs from the host DSH_HOME and dev-container
      docker volumes into the on-device agent-transcripts cache.
  - name: transcript-export-pi
    path: modules/agent-transcripts/skills/transcript-export-pi/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - observability
    command: false
    summary: Export pi coding-agent session JSONL from its settings and default session directories into
      the on-device agent-transcripts cache.
  - name: transcript-ingest
    path: modules/agent-transcripts/skills/transcript-ingest/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - observability
    command: false
    summary: Build or refresh the derived sqlite index over exported Claude Code, pi and dsh transcripts
      in the agent-transcripts cache.
  - name: transcript-query
    path: modules/agent-transcripts/skills/transcript-query/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - observability
    command: false
    summary: Query the agent-transcripts sqlite index to measure tool use, sessions, projects and activity
      across Claude Code, pi and dsh without loading transcript prose into context.
  - name: transcript-review
    path: modules/agent-transcripts/skills/transcript-review/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - observability
    command: false
    summary: Read one flagged agent session at a time and record what a deterministic sweep cannot see
      — a repeated failing approach, tool misuse, a user correction, a claim made without evidence — as
      validated findings in the shared findings database.
  - name: transcript-sweep
    path: modules/agent-transcripts/skills/transcript-sweep/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - observability
    command: false
    summary: Sweep the agent-transcripts sqlite index for sessions worth a closer look and record flagged
      tool errors, retry loops, duplicate calls, shell commands a dedicated tool covers, and oversized
      results or sessions in the findings store beside it.
- name: best-practices
  version: 0.4.3
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: Library of domain best-practices skills with stable, citable rule IDs (containers, uv,
    Python, FastAPI, databases, repo hygiene, Go, gRPC) plus the containers-audit interactive scan-and-fix
    workflow and the authoring tools for adding rules.
  skills:
  - name: containers-audit
    path: modules/best-practices/skills/containers-audit/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - audit
    - containers
    - docker
    command: true
    summary: Audit Dockerfile, compose and devcontainer.json files against the containers-best-practices
      rule set and offer each fix interactively.
  - name: containers-best-practices
    path: modules/best-practices/skills/containers-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when working with Dockerfiles, docker-compose/compose.yml, buildx, dev containers (devcontainer.json),
      or .dockerignore — including Python container builds with uv.
  - name: databases-best-practices
    path: modules/best-practices/skills/databases-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when working with SQLite or PostgreSQL (Postgres) from Python — sqlite3 connections,
      PRAGMA settings (journal_mode WAL, synchronous, foreign_keys, busy_timeout, optimize, user_version),
      SQLite schema and migrations, Postgres via psycopg and a SQLAlchemy pool, Alembic migrations, pg_advisory_xact_lock
      advisory locks, and SELECT FOR UPDATE row locking.
  - name: fastapi-best-practices
    path: modules/best-practices/skills/fastapi-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when working with FastAPI apps — building or reviewing APIRouter route modules, Pydantic
      v2 request/response models, Depends() dependencies, lifespan startup/shutdown, OAuth2/JWT or bearer-token
      auth, pydantic-settings secrets, async SQLAlchemy sessions, TestClient/httpx tests, uvicorn deployment,
      or observability (OpenTelemetry tracing/metrics, OTLP export, X-Request-ID request id and log correlation,
      /healthz and /readyz probes, pure ASGI middleware).
  - name: go-best-practices
    path: modules/best-practices/skills/go-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when working with Go code — .go files and go.mod/go.sum, go build / go test / go test
      -race / go vet / gofmt / staticcheck / golangci-lint / govulncheck, module and package layout, error
      handling (errors.Is / %w), the context package, goroutines and channels, errgroup, stdlib net/http
      servers and clients, graceful shutdown on SIGINT/SIGTERM, parameterized SQL, or Go security (html/template,
      crypto/rand).
  - name: grpc-best-practices
    path: modules/best-practices/skills/grpc-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when working with gRPC — .proto files and proto3 schema design, enum zero values and
      _UNSPECIFIED sentinels, well-known types (google.protobuf.Timestamp / Duration / FieldMask), List
      pagination (page_size / page_token / next_page_token), buf or protoc generation, grpc-go / grpclib
      / grpcio servers and clients, google.rpc status codes and rich errors, deadlines and cancellation
      propagation, streaming RPCs (server/client/bidi), interceptors (auth, logging, panic recovery, protovalidate),
      TLS / mTLS transport credentials and the insecure-credentials footgun, server reflection + grpcurl/grpcui,
      health checking, message-size limits and connection reuse.
  - name: meta-best-practices
    path: modules/best-practices/skills/meta-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when adding, editing, or reviewing rules in the best-practices library — covers the rule-ID
      format (PREFIX-NNN), severity scale, four-part What/Why/How/When-NOT-to-apply reference structure,
      SKILL.md shape, third-person description-field rules, and how to use tools/new-skill.sh + tools/new-rule.sh
      + tools/lint.sh + tools/render-index.sh.
  - name: python-best-practices
    path: modules/best-practices/skills/python-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when working with Python projects — `.py` files, `pyproject.toml` (without `[tool.uv]`),
      `conftest.py`, `mypy.ini`, `pyrightconfig.json`, `.python-version` — or when the user asks about
      Python project layout, typing, linting, testing, async, logging, packaging, dependency auditing,
      or profiling.
  - name: repo-best-practices
    path: modules/best-practices/skills/repo-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when editing repository hygiene and CI config — .pre-commit-config.yaml hooks, .github/dependabot.yml,
      GitHub Actions workflows (.github/workflows/*.yml, permissions, zizmor audits), CODEOWNERS, justfile
      task-runner recipes that CI calls, or secret scanning (gitleaks).
  - name: uv-best-practices
    path: modules/best-practices/skills/uv-best-practices/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Use when working with uv (Astral's Python package and project manager) — `uv sync` / `add`
      / `lock` / `run` / `tool` / `build` / `publish` / `audit` / `export`, `uvx`, a `uv.lock` or `pylock.toml`
      file, or a `pyproject.toml` with `[tool.uv]` or `[dependency-groups]`.
- name: browser
  version: 0.1.2
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: 'Drive a browser from any harness: headless auto mode, headed hybrid mode where a human
    logs in and the agent takes over, or attach to a browser the user opened via its debug port — through
    agent-browser on a persistent per-identity profile.'
  skills:
  - name: browser
    path: modules/browser/skills/browser/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - browser
    - web
    command: true
    summary: Drive a browser from any harness.
- name: code-review
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: 'Automated pull-request review on every harness: parallel reviewers from five lenses, confidence
    scoring that drops anything under 80, one comment posted with gh.'
  skills:
  - name: code-review
    path: modules/code-review/skills/code-review/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - github
    - review
    command: true
    summary: Review a GitHub pull request and post one comment with only the high-confidence issues.
- name: code-simplifier
  version: 0.1.0
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: Simplify recently changed code for clarity, consistency and maintainability without changing
    what it does, following the project's own guideline files.
  skills:
  - name: code-simplifier
    path: modules/code-simplifier/skills/code-simplifier/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - quality
    - refactor
    command: true
    summary: Simplify recently changed code for clarity, consistency and maintainability without changing
      what it does.
- name: dual-agent-pr-review
  version: 0.1.4
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  description: Review a GitHub PR with Claude Code and Codex in parallel, then reconcile their findings
    into one consensus set.
  skills:
  - name: dual-agent-pr-review
    path: modules/dual-agent-pr-review/skills/dual-agent-pr-review/SKILL.md
    harness:
    - claude
    tags: []
    command: true
    summary: Review a GitHub PR with two independent CLI reviewers (Claude Code and Codex) in parallel,
      then reconcile their findings into one evidence-backed consensus set.
- name: feature-dev
  version: 0.1.2
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: 'Guided seven-phase feature development: explore the codebase with parallel specialist
    agents, ask clarifying questions, compare architectures, implement, then review. A harness-neutral
    port of Anthropic''s feature-dev plugin whose agents dispatch on Claude Code, pi and dsh.'
  skills:
  - name: feature-dev
    path: modules/feature-dev/skills/feature-dev/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: true
    summary: Guided seven-phase feature development with codebase understanding and architecture focus.
- name: plan-for-dummies
  version: 0.3.1
  kinds:
  - skills
  - dsh-plugin
  harness:
  - claude
  - pi
  - dsh
  description: Write an implementation plan that a less capable model (Sonnet, Haiku, a cheap subagent)
    will execute without the planner present.
  skills:
  - name: plan-for-dummies
    path: modules/plan-for-dummies/skills/plan-for-dummies/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags: []
    command: false
    summary: Write an implementation plan that a less capable model (Sonnet, Haiku, a cheap subagent)
      will execute without the planner present.
- name: pr-flow
  version: 0.2.6
  kinds:
  - skills
  - dsh-plugin
  - hooks
  harness:
  - claude
  - pi
  - dsh
  description: 'Work off main on every harness: branch + worktree, PR, watch CI until green, merge only
    when told, tear down after merge.'
  skills:
  - name: pr-flow
    path: modules/pr-flow/skills/pr-flow/SKILL.md
    harness:
    - claude
    - dsh
    - pi
    tags:
    - git
    - workflow
    command: true
    summary: Use whenever a task will change files in a git repository.
