# Best-practices library — rule index

_Auto-generated by `tools/render-index.sh`. Do not edit by hand._

Every rule across every skill, with links into the reference
files. Skills are listed alphabetically by directory name.

## containers-audit

_Audit Dockerfile, compose and devcontainer.json files against the containers-best-practices rule set and offer each fix interactively. Walks every container file under a path, prints a prioritized findings report keyed by rule ID (DOCKER-, COMPOSE-, DEVC-, BUILDX-, SEC-, UV-), then applies fixes one at a time with per-finding approval. Use when asked to audit, scan, lint or harden containers, Dockerfiles, compose files or dev containers; never autofixes._

_(no rules listed in SKILL.md rule index yet)_

## containers-best-practices

_Use when working with Dockerfiles, docker-compose/compose.yml, buildx, dev containers (devcontainer.json), or .dockerignore — including Python container builds with uv. Covers image hardening, multi-stage builds, layer/cache ordering, BuildKit cache + secret mounts, compose services, GPU + Docker Model Runner, dev-container lifecycle and volume state, and container security footguns. For Python packaging outside containers use python-best-practices or uv-best-practices._

| ID | Summary |
|---|---|
| [`DOCKER-001`](skills/containers-best-practices/references/dockerfile.md#docker-001) | Use multi-stage builds for any compiled or dependency-heavy app. |
| [`DOCKER-002`](skills/containers-best-practices/references/dockerfile.md#docker-002) | Pin base images by digest (`image@sha256:...`), not just tag. |
| [`DOCKER-003`](skills/containers-best-practices/references/dockerfile.md#docker-003) | Order layers cache-friendly: dependency manifests first, source last. |
| [`DOCKER-004`](skills/containers-best-practices/references/dockerfile.md#docker-004) | `COPY` only what's needed; rely on `.dockerignore` to keep context small. |
| [`DOCKER-005`](skills/containers-best-practices/references/dockerfile.md#docker-005) | Run as a non-root user in the final stage. |
| [`DOCKER-006`](skills/containers-best-practices/references/dockerfile.md#docker-006) | Use `tini`/`dumb-init` (or run with `--init`) for proper PID 1 / signal handling. |
| [`DOCKER-007`](skills/containers-best-practices/references/dockerfile.md#docker-007) | Add `HEALTHCHECK` for long-lived services. |
| [`DOCKER-009`](skills/containers-best-practices/references/dockerfile.md#docker-009) | Use `RUN --mount=type=cache` for package managers under BuildKit. |
| [`DOCKER-010`](skills/containers-best-practices/references/dockerfile.md#docker-010) | Never bake secrets into layers; use `RUN --mount=type=secret`. |
| [`DOCKER-011`](skills/containers-best-practices/references/dockerfile.md#docker-011) | Minimize the final image: distroless / alpine / slim — choose with eyes open. |
| [`DOCKER-012`](skills/containers-best-practices/references/dockerfile.md#docker-012) | Set `WORKDIR` explicitly; never rely on `/`. |
| [`DOCKER-013`](skills/containers-best-practices/references/dockerfile.md#docker-013) | Prefer `ENTRYPOINT` (exec form) + `CMD` over shell-form commands. |
| [`DOCKER-014`](skills/containers-best-practices/references/dockerfile.md#docker-014) | Use HereDoc syntax (`RUN <<EOF ... EOF`) for multi-line `RUN` blocks. |
| [`DOCKER-015`](skills/containers-best-practices/references/dockerfile.md#docker-015) | Use `set -o pipefail` (or exec-form bash) for piped `RUN` commands. |
| [`DOCKER-016`](skills/containers-best-practices/references/dockerfile.md#docker-016) | Prefer `COPY --link` for COPYs that don't depend on prior `RUN` mutations — produces independent overlay layers that survive base-image rebases. `--link` is per-instruction, not a stage-wide rule. |
| [`DOCKER-017`](skills/containers-best-practices/references/dockerfile.md#docker-017) | Scope BuildKit cache mount `id=` by `${TARGETARCH}` for multi-platform builds. |
| [`DOCKER-018`](skills/containers-best-practices/references/dockerfile.md#docker-018) | Remove the `docker-clean` snippet from the image's apt.conf.d before mounting an APT cache (otherwise the cache is silently wiped after every install). |
| [`DOCKER-019`](skills/containers-best-practices/references/dockerfile.md#docker-019) | `apt-get update` and `apt-get install` must share a single `RUN` — split layers cache the package index separately and serve stale data on rebuild. |
| [`DOCKER-020`](skills/containers-best-practices/references/dockerfile.md#docker-020) | Pass `--no-install-recommends` to `apt-get install` (and the RHEL `--setopt=install_weak_deps=False` equivalent) — recommends pull in tens of MB of unneeded packages. |
| [`DOCKER-021`](skills/containers-best-practices/references/dockerfile.md#docker-021) | `pip install` either takes `--no-cache-dir` or wraps in a BuildKit cache mount; the default wheel cache bloats the layer by hundreds of MB. |
| [`DOCKER-022`](skills/containers-best-practices/references/dockerfile.md#docker-022) | Replace deprecated `MAINTAINER` with `LABEL org.opencontainers.image.authors=...` / `.vendor=...`. |
| [`DOCKER-023`](skills/containers-best-practices/references/dockerfile.md#docker-023) | Declare OCI image labels (`org.opencontainers.image.source`, `.revision`, `.version`, `.created`, `.licenses`) for traceability from registry back to git SHA. |
| [`DOCKER-024`](skills/containers-best-practices/references/dockerfile.md#docker-024) | Avoid `chmod 777` / `chmod -R 777`; almost always masks a UID mismatch that wants `chown` instead. |
| [`DOCKER-025`](skills/containers-best-practices/references/dockerfile.md#docker-025) | `EXPOSE` must agree with the port `CMD`/`ENTRYPOINT` actually binds; drift breaks `docker run -P`, compose `expose:`, and reverse-proxy auto-discovery. |
| [`DOCKER-026`](skills/containers-best-practices/references/dockerfile.md#docker-026) | Never `curl ... \| sh` without checksum verification — fetch remote artifacts with `ADD --checksum=sha256:...` (Dockerfile ≥1.6) or verify `sha256sum` before executing. |
| [`DOCKER-027`](skills/containers-best-practices/references/dockerfile.md#docker-027) | Pin `# syntax=docker/dockerfile:1` at the top of every Dockerfile; without it, BuildKit features silently degrade. Don't pin an older minor like `:1.8` — that's a downgrade. |
| [`DOCKER-028`](skills/containers-best-practices/references/dockerfile.md#docker-028) | Use `COPY --exclude=<pattern>` (Dockerfile ≥1.19) to filter unwanted files from multi-file copies instead of `COPY` + `RUN rm` or multi-COPY workarounds. |
| [`DOCKER-029`](skills/containers-best-practices/references/dockerfile.md#docker-029) | Run `docker build --check` as a Dockerfile lint step in CI (Buildx ≥0.15, Dockerfile ≥1.8); it exits non-zero on violations, and `# check=error=true` fails ordinary builds too. |
| [`DOCKER-030`](skills/containers-best-practices/references/dockerfile.md#docker-030) | Make image builds bit-for-bit reproducible via `SOURCE_DATE_EPOCH` + `--output type=image,...,rewrite-timestamp=true` (BuildKit ≥0.13). |
| [`DOCKER-031`](skills/containers-best-practices/references/dockerfile.md#docker-031) | Use `COPY --parents` (Dockerfile ≥1.20) to preserve source directory structure instead of flattening or post-copy `mkdir`. |
| [`DOCKER-032`](skills/containers-best-practices/references/dockerfile.md#docker-032) | Cap heavy `RUN` steps with build-time resource limits (`--resource cpu=,memory=`) on shared builders. |
| [`DOCKER-033`](skills/containers-best-practices/references/dockerfile.md#docker-033) | Pin `RUN --network=none` on steps that must not touch the network, to enforce reproducibility. |
| [`DEVC-001`](skills/containers-best-practices/references/devcontainer.md#devc-001) | Map host UID/GID to avoid file-ownership pain on bind mounts (`updateRemoteUserUID`; it skips taken UIDs, is off on macOS, only re-owns `$HOME`, and misfires under rootless Docker). |
| [`DEVC-002`](skills/containers-best-practices/references/devcontainer.md#devc-002) | Put long-running installs in the **image**, not `postCreateCommand`. |
| [`DEVC-003`](skills/containers-best-practices/references/devcontainer.md#devc-003) | Use official `features` for common tooling instead of hand-rolling; `:1` pins the feature major, the `version` option is the tool version. |
| [`DEVC-004`](skills/containers-best-practices/references/devcontainer.md#devc-004) | Pick the right lifecycle hook: `onCreateCommand` vs `updateContentCommand` vs `postCreateCommand` vs `postStartCommand` vs `postAttachCommand`. |
| [`DEVC-005`](skills/containers-best-practices/references/devcontainer.md#devc-005) | Mount cache volumes for language package managers (uv, pnpm, go module cache, cargo, pip). |
| [`DEVC-006`](skills/containers-best-practices/references/devcontainer.md#devc-006) | Be deliberate about `mounts` and `workspaceFolder` — never mount key files or credential dirs; SSH goes through the agent (DEVC-019). |
| [`DEVC-007`](skills/containers-best-practices/references/devcontainer.md#devc-007) | Choose `remoteUser` vs `containerUser` consciously; on images that supply a non-root user (mcr devcontainers images do it via their metadata label) set neither. |
| [`DEVC-008`](skills/containers-best-practices/references/devcontainer.md#devc-008) | Don't commit secrets in `devcontainer.json`; use `${localEnv:...}` or a mounted env file. |
| [`DEVC-009`](skills/containers-best-practices/references/devcontainer.md#devc-009) | Declare required `customizations.vscode.extensions` and settings the project assumes. |
| [`DEVC-010`](skills/containers-best-practices/references/devcontainer.md#devc-010) | Lifecycle scripts must be idempotent (they re-run on rebuild). |
| [`DEVC-011`](skills/containers-best-practices/references/devcontainer.md#devc-011) | Prefer `image` or `build` + a pinned base; avoid bare `Dockerfile` references that resolve ambiguously. |
| [`DEVC-012`](skills/containers-best-practices/references/devcontainer.md#devc-012) | Cross-platform host mount paths use `${localEnv:HOME}${localEnv:USERPROFILE}`; an empty or missing bind source is a hard mount error, and `${localEnv:VAR:default}` covers only an unset variable. |
| [`DEVC-013`](skills/containers-best-practices/references/devcontainer.md#devc-013) | Two-tier named volume naming: `<repo>-<tool>` for shared caches, `<repo>-<purpose>-${devcontainerId}` for per-worktree state (`.venv`, history, tool auth). |
| [`DEVC-014`](skills/containers-best-practices/references/devcontainer.md#devc-014) | On macOS, mount `.venv` (and other heavy tool-generated dirs) as a named volume — bind-mounted Python venvs are dramatically slower through VirtioFS. On Linux too when host and container both run uv: a shared venv symlinks an interpreter missing on the other side, so each side deletes and rebuilds it. |
| [`DEVC-015`](skills/containers-best-practices/references/devcontainer.md#devc-015) | Persist agentic CLI tool state (the home-directory `.claude`, `.codex`, `.config/gh` dirs, etc.) in per-worktree named volumes. Don't bind-mount these from the host — leaks tokens and conversation history both directions. Don't omit the mount — every rebuild wipes login state. |
| [`DEVC-016`](skills/containers-best-practices/references/devcontainer.md#devc-016) | Use `forwardPorts` + `portsAttributes` for editor port forwarding, but the devcontainer CLI ignores them — use `appPort` (published on `127.0.0.1`) for headless use; `runArgs` only for flags the spec doesn't model. |
| [`DEVC-017`](skills/containers-best-practices/references/devcontainer.md#devc-017) | Add capabilities deliberately via the first-class `capAdd` / `securityOpt` properties (they also reach compose, unlike `runArgs`); never default to `privileged`. |
| [`DEVC-018`](skills/containers-best-practices/references/devcontainer.md#devc-018) | Set `"init": true` at the devcontainer.json level (or `services.<x>.init: true` in compose) so signals + zombie reaping work — same DOCKER-006 rationale, but at the dev-container layer. |
| [`DEVC-019`](skills/containers-best-practices/references/devcontainer.md#devc-019) | SSH and git credentials: bind the host ssh-agent socket (the CLI never forwards it), never key files or the host SSH dir; pin known_hosts; strip credential helpers and host pagers from a copied gitconfig. |
| [`DEVC-020`](skills/containers-best-practices/references/devcontainer.md#devc-020) | Use the top-level `secrets` property to declare *recommended* secret names (Codespaces offers an optional prompt; the devcontainer CLI ignores it); `--secrets-file` feeds lifecycle commands only. |
| [`DEVC-021`](skills/containers-best-practices/references/devcontainer.md#devc-021) | Commit `devcontainer-lock.json` (stable, generated by default in CLI ≥0.87.0) for reproducible feature versions; use `--frozen-lockfile` in CI. |
| [`DEVC-022`](skills/containers-best-practices/references/devcontainer.md#devc-022) | Declare `hostRequirements.gpu` for GPU dev containers; don't hard-code `runArgs --gpus all` (the CLI adds it when it detects a GPU). |
| [`DEVC-023`](skills/containers-best-practices/references/devcontainer.md#devc-023) | `containerEnv` reaches every process (`docker exec`, agents); `remoteEnv` only tool-spawned ones; `${containerEnv:VAR}` is valid only in `remoteEnv`. |
| [`DEVC-024`](skills/containers-best-practices/references/devcontainer.md#devc-024) | Configs started headless (CLI, CI, agents) must do what VS Code does for you: agent forwarding, gitconfig, known_hosts, port forwarding, GPG. |
| [`DEVC-025`](skills/containers-best-practices/references/devcontainer.md#devc-025) | A Docker socket (docker-outside-of-docker) is root on the host — every credential boundary is moot with it, especially with auto-approve agents; make it an explicit, documented choice. |
| [`DEVC-026`](skills/containers-best-practices/references/devcontainer.md#devc-026) | Pin mcr dev container images to `<image-major>-<lang>-<os>` (e.g. `python:3-3.14-trixie`) or a digest; short tags float across image majors and OS releases, which breaks features. |
| [`COMPOSE-001`](skills/containers-best-practices/references/compose.md#compose-001) | Use `depends_on` with `condition: service_healthy`, not bare `depends_on`; add `restart: true` (≥2.17) to restart on dependency updates and `required: false` (≥2.20) for optional dependencies. |
| [`COMPOSE-002`](skills/containers-best-practices/references/compose.md#compose-002) | Pick named volumes vs bind mounts deliberately; know when each is correct. |
| [`COMPOSE-003`](skills/containers-best-practices/references/compose.md#compose-003) | Use `env_file` instead of inline `environment` for secrets-adjacent values. |
| [`COMPOSE-004`](skills/containers-best-practices/references/compose.md#compose-004) | Gate optional services with `profiles` (tools, debug, monitoring). |
| [`COMPOSE-005`](skills/containers-best-practices/references/compose.md#compose-005) | Commit `compose.override.yml` as the shared dev overlay; prod uses `-f`, personal tweaks a gitignored `compose.local.yml`. |
| [`COMPOSE-006`](skills/containers-best-practices/references/compose.md#compose-006) | Compose v2 ignores the top-level `version:` key — remove it from new files. |
| [`COMPOSE-007`](skills/containers-best-practices/references/compose.md#compose-007) | Define explicit `networks` for multi-service apps; don't rely on the default bridge for everything. |
| [`COMPOSE-009`](skills/containers-best-practices/references/compose.md#compose-009) | Use `restart: unless-stopped` for long-lived services; it differs from `always` only in keeping a manually stopped container stopped across a daemon restart. |
| [`COMPOSE-010`](skills/containers-best-practices/references/compose.md#compose-010) | Use YAML anchors / `extends` instead of duplicating service definitions. |
| [`COMPOSE-011`](skills/containers-best-practices/references/compose.md#compose-011) | Healthchecks belong on the services that need to be waited on, not the waiters. |
| [`COMPOSE-012`](skills/containers-best-practices/references/compose.md#compose-012) | One-shot init/migration containers use `restart: "no"` and downstream services depend on them with `condition: service_completed_successfully`; `docker compose up` re-runs them every time, so they must be idempotent. |
| [`COMPOSE-013`](skills/containers-best-practices/references/compose.md#compose-013) | Postgres: mount the data volume at `/var/lib/postgresql/data` for ≤17 and at `/var/lib/postgresql` for 18+ (version-qualified `PGDATA`); the wrong level silently writes to an anonymous volume. |
| [`COMPOSE-014`](skills/containers-best-practices/references/compose.md#compose-014) | Reverse-proxy healthchecks hit a *local* endpoint (e.g. nginx `location = /__healthz`), not a path that proxies upstream — otherwise backend slowness restarts the proxy. |
| [`COMPOSE-015`](skills/containers-best-practices/references/compose.md#compose-015) | Use `develop.watch` (Compose ≥ 2.22) for hot-reload — `sync` / `rebuild` / `restart` / `sync+restart` / `sync+exec` per path, plus `initial_sync`, beats bare bind-mounting the workspace. |
| [`COMPOSE-016`](skills/containers-best-practices/references/compose.md#compose-016) | Use the top-level `configs:` block for non-secret config files; pairs cleanly with `secrets:` (SEC-009) and avoids the "single-file bind mount becomes a directory" footgun. |
| [`COMPOSE-017`](skills/containers-best-practices/references/compose.md#compose-017) | Set `pull_policy` explicitly on shared / production stacks; the default `missing` silently runs stale images for mutable tags other than `latest` (which is always pulled). `daily` / `weekly` / `every_<duration>` sit in between. |
| [`COMPOSE-018`](skills/containers-best-practices/references/compose.md#compose-018) | Know the two unrelated `.env` mechanisms: project-dir `.env` (parse-time `${VAR}` substitution into compose.yml) vs `env_file:` (runtime container env). `--env-file` only affects the first. |
| [`COMPOSE-019`](skills/containers-best-practices/references/compose.md#compose-019) | Use the `include:` directive (Compose ≥2.20) to compose modular sub-applications into a parent project; distinct from `extends:` (single-service) and `-f` (file-level merge). Remote `oci://` sources need Compose ≥2.34 (and COMPOSE-028). |
| [`COMPOSE-020`](skills/containers-best-practices/references/compose.md#compose-020) | Use the `gpus:` shorthand (Compose ≥2.30) for GPU services; replaces the verbose `deploy.resources.reservations.devices` form. |
| [`COMPOSE-021`](skills/containers-best-practices/references/compose.md#compose-021) | Use the top-level `models:` block (Compose ≥2.38) for declarative AI-model dependencies via Docker Model Runner (Docker Engine or Desktop); `<KEY>_URL` / `<KEY>_MODEL` injected as env vars. |
| [`COMPOSE-023`](skills/containers-best-practices/references/compose.md#compose-023) | Set `security_opt: ["no-new-privileges:true"]` on production services; closes the setuid-escalation path that DOCKER-005 alone doesn't. |
| [`COMPOSE-026`](skills/containers-best-practices/references/compose.md#compose-026) | Declare `logging:` on long-lived services — prefer the `local` driver (rotates by default); on `json-file` set `max-size` + `max-file`, since it has no size limit and silently fills the host disk. |
| [`COMPOSE-027`](skills/containers-best-practices/references/compose.md#compose-027) | Use `post_start` / `pre_stop` lifecycle hooks (Compose ≥2.30) for privileged setup/teardown without weakening the service. |
| [`COMPOSE-028`](skills/containers-best-practices/references/compose.md#compose-028) | Require Compose ≥2.40.2 (or v5) before resolving any remote OCI compose artifact (`include: oci://`, `-f oci://`) — CVE-2025-62725 path-traversal via artifact layer annotations. |
| [`COMPOSE-029`](skills/containers-best-practices/references/compose.md#compose-029) | Set `init: true` on services whose image runs the app as PID 1 without an init — signals get forwarded and zombies reaped; doesn't replace DOCKER-006 for images you build. |
| [`BUILDX-001`](skills/containers-best-practices/references/buildx.md#buildx-001) | Pick a cache backend per scenario: `gha` for GitHub Actions, `registry` for portable CI, `inline` for simplest-but-limited, `local` for single-machine. |
| [`BUILDX-002`](skills/containers-best-practices/references/buildx.md#buildx-002) | Cache reuse requires **both** `--cache-from` and `--cache-to`. Specify `--cache-from` multiple times (branch + main fallback). Use `mode=max` for multi-stage builds where intermediate layers matter. |
| [`BUILDX-003`](skills/containers-best-practices/references/buildx.md#buildx-003) | Choose `--load` (single-arch, into local daemon), `--push` (registry, multi-arch OK), or `--output type=...` (OCI archive, local dir). Without one of these, multi-arch builds run and **discard** the result. |
| [`BUILDX-004`](skills/containers-best-practices/references/buildx.md#buildx-004) | Use `docker buildx bake` for multi-target / declarative builds — replaces long CLI commands, reads `compose.yaml` directly, builds all targets in parallel. |
| [`BUILDX-005`](skills/containers-best-practices/references/buildx.md#buildx-005) | The default `docker` driver exports caches and builds multi-platform images only with the containerd image store, and never exports tarballs. Switch to `docker-container` (or `kubernetes`) for full BuildKit features. `setup-buildx-action` does this for you in CI. |
| [`BUILDX-006`](skills/containers-best-practices/references/buildx.md#buildx-006) | For non-trivial multi-arch builds, use native runners per architecture (multi-node builder, or separate CI jobs + `imagetools create`). QEMU emulation is much slower for CPU-bound builds. |
| [`BUILDX-007`](skills/containers-best-practices/references/buildx.md#buildx-007) | Use `docker buildx debug build` (still experimental) — or `docker buildx dap build` (out of experimental since buildx v0.33.0) for editor integration — for interactive step-through of failing builds; replaces the `RUN sleep 999` + `docker exec` hack. |
| [`BUILDX-008`](skills/containers-best-practices/references/buildx.md#buildx-008) | Min-mode provenance is attached by default and already makes the push an index; registries that can't store one need `--provenance=false` / `BUILDX_NO_DEFAULT_ATTESTATIONS=1`, not `mode=min`. `mode=max` records build-arg values. |
| [`BUILDX-009`](skills/containers-best-practices/references/buildx.md#buildx-009) | Use `bake` matrix targets + composable HCL attributes for multi-variant builds. |
| [`BUILDX-010`](skills/containers-best-practices/references/buildx.md#buildx-010) | Pin the Docker GitHub Actions (`setup-buildx-action@v4`, `build-push-action@v7`, `bake-action@v7`) by commit SHA. |
| [`SEC-001`](skills/containers-best-practices/references/security-build.md#sec-001) | Always have a `.dockerignore`. At minimum: `.git`, `node_modules`, `__pycache__`, `.venv`, `.env*`, `dist/`, build artifacts. |
| [`SEC-002`](skills/containers-best-practices/references/security-build.md#sec-002) | Use BuildKit (the default builder since Docker Engine 23.0, or `docker buildx`). Many modern features depend on it. |
| [`SEC-004`](skills/containers-best-practices/references/security-build.md#sec-004) | Build multi-platform with `buildx` only when needed; single-platform is cheaper. |
| [`SEC-005`](skills/containers-best-practices/references/security-build.md#sec-005) | Never leak `.git`, lockfile-only artifacts, or `node_modules` into images. |
| [`SEC-006`](skills/containers-best-practices/references/security-build.md#sec-006) | Use BuildKit secret mounts for build-time credentials (`env=` exposes one as a variable, Dockerfile ≥1.10); never `ARG` them — build args persist in image history and provenance. |
| [`SEC-008`](skills/containers-best-practices/references/security-build.md#sec-008) | Minimize `RUN` layers without sacrificing readability; combine related steps, not unrelated ones. |
| [`SEC-009`](skills/containers-best-practices/references/security-build.md#sec-009) | Use Compose's `secrets:` block (mounted as files under `/run/secrets/`) instead of `environment:` for any sensitive value — env vars leak via `docker inspect`, `/proc/<pid>/environ`, and logs. `file:` secrets are world-readable `0444` bind mounts; `uid`/`gid`/`mode` are ignored for them. |
| [`SEC-010`](skills/containers-best-practices/references/security-build.md#sec-010) | Image references with `@sha256:` digest must be fully literal; don't templatize the digest in Jinja/copier (template drift makes the version+digest pair inconsistent) and don't split version + digest across separate variables. A template's version choices are each a complete literal `image:tag@sha256` string kept current by Renovate. |
| [`SEC-011`](skills/containers-best-practices/references/security-build.md#sec-011) | Add `.venv`, `node_modules`, `target/`, `.gradle/`, tool caches and other platform-specific build dirs to `.dockerignore` — they're at best slow, at worst architecture-incompatible inside the image. |
| [`SEC-012`](skills/containers-best-practices/references/security-build.md#sec-012) | Never `COPY .env` / `COPY *.env` into images; explicit `COPY` bypasses `.dockerignore` and bakes secrets into a layer permanently. |
| [`SEC-013`](skills/containers-best-practices/references/security-build.md#sec-013) | `.git/` must be in `.dockerignore` and never `COPY`'d into the final image — leaks full repo history, rotated secrets, internal codenames. |
| [`SEC-014`](skills/containers-best-practices/references/security-build.md#sec-014) | Flag `ARG`/`ENV` names matching `*SECRET*` / `*TOKEN*` / `*PASSWORD*` / `*KEY*` / `*CREDENTIAL*` — values get baked into image metadata visible via `docker history` / `docker inspect`. |
| [`SEC-015`](skills/containers-best-practices/references/security-build.md#sec-015) | `.dockerignore` must be *comprehensive*, not just present; check coverage against a baseline (VCS, secrets, per-ecosystem build dirs, tool caches, editor/OS, Docker meta). |
| [`SEC-016`](skills/containers-best-practices/references/security-build.md#sec-016) | Broad `COPY . .` is only safe when `.dockerignore` passes SEC-015; without comprehensive ignores, prefer narrow per-subtree copies. |
| [`SEC-017`](skills/containers-best-practices/references/security-build.md#sec-017) | Prefer `COPY` over `ADD`; flag only un-checksummed `ADD <url>` (silent supply chain) and implicit local tar auto-extract. `ADD --checksum=` is the recommended remote fetch. |
| [`SEC-019`](skills/containers-best-practices/references/security-build.md#sec-019) | `.env` files must be in `.gitignore` (not only `.dockerignore`) *and* must not already be git-tracked — `git rm --cached` is required to actually untrack a previously-committed `.env`. |
| [`SEC-020`](skills/containers-best-practices/references/security-build.md#sec-020) | Use `RUN --mount=type=ssh` (or `ADD git@...`) + `docker buildx build --ssh default` for SSH-authenticated git/dep access during builds, or a host-scoped `GIT_AUTH_TOKEN.<host>` secret for HTTPS; never `COPY id_rsa` or `ARG SSH_PRIVATE_KEY`. |
| [`SEC-021`](skills/containers-best-practices/references/security-build.md#sec-021) | Pin third-party scanner/tool images by digest and CI Actions by commit SHA — floating tags get hijacked (Trivy supply-chain compromise, CVE-2026-33634). |
| [`SEC-022`](skills/containers-best-practices/references/security-build.md#sec-022) | Sign images with keyless cosign and verify with explicit `--certificate-identity` + `--certificate-oidc-issuer` (Docker Content Trust was removed in Engine 29). |
| [`SEC-023`](skills/containers-best-practices/references/security-build.md#sec-023) | Prefer a hardened/minimal verified base (Docker Hardened Images — Apache-2.0, distroless runtime variants — Chainguard, distroless) for production runtimes. |
| [`SEC-024`](skills/containers-best-practices/references/security-build.md#sec-024) | Run the daemon rootless (or with userns-remap) on hosts that run untrusted workloads; neither replaces DOCKER-005. |
| [`UV-001`](skills/containers-best-practices/references/uv-python.md#uv-001) | Install `uv` via `COPY --from=ghcr.io/astral-sh/uv:VERSION@sha256:DIGEST /uv /uvx /bin/`. Pin the literal version+digest pair (SEC-010) and verify it with `gh attestation verify`; the uv image is distroless, so the copy carries no extra weight. |
| [`UV-002`](skills/containers-best-practices/references/uv-python.md#uv-002) | Two-pass sync: bind-mount manifests + `uv sync --locked --no-install-project` first; then copy source + `uv sync --locked`. Dependency layer caches independently from source edits. Declare `ARG TARGETARCH` in the stage before using it in a cache `id=`. |
| [`UV-003`](skills/containers-best-practices/references/uv-python.md#uv-003) | Set `ENV UV_LINK_MODE=copy` whenever a BuildKit cache mount is used — silences the cross-filesystem hard-link warning that fires on every install otherwise. |
| [`UV-004`](skills/containers-best-practices/references/uv-python.md#uv-004) | Set `ENV UV_COMPILE_BYTECODE=1` (or pass `--compile-bytecode`) for production images; trades a bit of build time for noticeably faster cold starts. |
| [`UV-005`](skills/containers-best-practices/references/uv-python.md#uv-005) | `--locked` asserts the lockfile is up to date with `pyproject.toml`; `--frozen` skips that check. Default to `--locked` everywhere; use `--frozen` only in workspace pass-1 where members aren't yet present. |
| [`UV-006`](skills/containers-best-practices/references/uv-python.md#uv-006) | For minimal final images, use the copy-from-builder pattern: install with `--no-editable` in a builder stage, then `COPY --from=builder /app/.venv /app/.venv` into a clean final stage — source doesn't ship. |
| [`UV-007`](skills/containers-best-practices/references/uv-python.md#uv-007) | Set `UV_PYTHON_DOWNLOADS=0` (use the base image's Python, don't download another) and optionally `UV_PROJECT_ENVIRONMENT` pointed at the system prefix (install into system Python, skip the `.venv` indirection) for system-Python builds. |
| [`UV-008`](skills/containers-best-practices/references/uv-python.md#uv-008) | Workspace pattern: pass 1 uses `uv sync --frozen --no-install-workspace` (members not yet copied), pass 2 uses `uv sync --locked` after the source is in place. |
| [`UV-009`](skills/containers-best-practices/references/uv-python.md#uv-009) | Point `UV_TOOL_BIN_DIR` at the system bin dir for derived images that install CLI tools via `uv tool install`, so the binaries land on `PATH` instead of uv's default executable dir (`$HOME/.local/bin`). |
| [`UV-010`](skills/containers-best-practices/references/uv-python.md#uv-010) | Use `ENV UV_NO_DEV=1` and the `UV_NO_INSTALL_*` env vars instead of repeating flags across the two-pass sync. |
| [`UV-011`](skills/containers-best-practices/references/uv-python.md#uv-011) | Migrate off the removed `bookworm` uv base tags to `trixie` (uv 0.10.0 dropped `bookworm`/`bookworm-slim`). |

## databases-best-practices

_Use when working with SQLite or PostgreSQL (Postgres) from Python — sqlite3 connections, PRAGMA settings (journal_mode WAL, synchronous, foreign_keys, busy_timeout, optimize, user_version), SQLite schema and migrations, Postgres via psycopg and a SQLAlchemy pool, Alembic migrations, pg_advisory_xact_lock advisory locks, and SELECT FOR UPDATE row locking. Covers the SQLITE- and PG- rule families. Async SQLAlchemy session wiring in FastAPI lives in fastapi-best-practices._

| ID | Summary |
|---|---|
| [`SQLITE-001`](skills/databases-best-practices/references/sqlite.md#sqlite-001) | Set `journal_mode=WAL` on the writer and check the mode it returns. |
| [`SQLITE-002`](skills/databases-best-practices/references/sqlite.md#sqlite-002) | Use `synchronous=NORMAL` with WAL: never corrupt, but the last commits can be lost on power loss. |
| [`SQLITE-003`](skills/databases-best-practices/references/sqlite.md#sqlite-003) | Give every connection a busy timeout (in Python, `connect(timeout=)` is it). |
| [`SQLITE-004`](skills/databases-best-practices/references/sqlite.md#sqlite-004) | Turn `foreign_keys` on for every connection; it's off by default. |
| [`SQLITE-005`](skills/databases-best-practices/references/sqlite.md#sqlite-005) | Start writes with `BEGIN IMMEDIATE`; roll back a failed `COMMIT` only if `in_transaction`. |
| [`SQLITE-006`](skills/databases-best-practices/references/sqlite.md#sqlite-006) | One writer connection and a pool of `mode=ro` readers, each read in one explicit transaction. |
| [`SQLITE-007`](skills/databases-best-practices/references/sqlite.md#sqlite-007) | Run `PRAGMA optimize = 0x10002` at open (after migrations), periodically, and at close. |
| [`SQLITE-008`](skills/databases-best-practices/references/sqlite.md#sqlite-008) | Key migrations on `PRAGMA user_version`, each step and its bump in one `BEGIN IMMEDIATE` transaction. |
| [`SQLITE-009`](skills/databases-best-practices/references/sqlite.md#sqlite-009) | Pass every value as a bound parameter. |
| [`SQLITE-010`](skills/databases-best-practices/references/sqlite.md#sqlite-010) | Use `STRICT` tables, `CHECK` constraints, and money as integer cents. |
| [`SQLITE-011`](skills/databases-best-practices/references/sqlite.md#sqlite-011) | Use `sqlite3` with `autocommit=True` (3.12+) and `check_same_thread=False`, one thread per connection at a time. |
| [`SQLITE-012`](skills/databases-best-practices/references/sqlite.md#sqlite-012) | Set `cache_size` (negative means KiB) and `temp_store=MEMORY` on every connection. |
| [`SQLITE-013`](skills/databases-best-practices/references/sqlite.md#sqlite-013) | Batch many writes into one transaction (`executemany`). |
| [`SQLITE-014`](skills/databases-best-practices/references/sqlite.md#sqlite-014) | Know when not to use SQLite (network filesystems, many hosts or writers); leave `mmap_size` off unless measured. |
| [`SQLITE-015`](skills/databases-best-practices/references/sqlite.md#sqlite-015) | Store timestamps as UTC ISO-8601 `TEXT` generated by SQLite (`strftime`). |
| [`PG-001`](skills/databases-best-practices/references/postgres.md#pg-001) | Size the per-process pool against `max_connections`; pre-ping and recycle pooled connections. |
| [`PG-003`](skills/databases-best-practices/references/postgres.md#pg-003) | Serialize concurrent migrators with `pg_advisory_xact_lock` inside Alembic's migration transaction. |
| [`PG-004`](skills/databases-best-practices/references/postgres.md#pg-004) | Test that the models match the migrated schema (`compare_metadata`); review autogenerated migrations. |
| [`PG-005`](skills/databases-best-practices/references/postgres.md#pg-005) | Test each migration's upgrade → downgrade → upgrade round trip against the real Postgres image. |
| [`PG-013`](skills/databases-best-practices/references/postgres.md#pg-013) | Lock every row a transaction will change with `SELECT … FOR UPDATE`, in one consistent order (`ORDER BY id`). |

## fastapi-best-practices

_Use when working with FastAPI apps — building or reviewing APIRouter route modules, Pydantic v2 request/response models, Depends() dependencies, lifespan startup/shutdown, OAuth2/JWT or bearer-token auth, pydantic-settings secrets, async SQLAlchemy sessions, TestClient/httpx tests, uvicorn deployment, or observability (OpenTelemetry tracing/metrics, OTLP export, X-Request-ID request id and log correlation, /healthz and /readyz probes, pure ASGI middleware). Covers the FAPI- rule family (structure, models/validation, settings, dependencies, path operations, errors, security, async DB, testing, deployment), OBS- (observability) and API- (bearer-token comparison, secret-file precedence). FastAPI-specific only; general Python rules live in python-best-practices._

| ID | Summary |
|---|---|
| [`FAPI-001`](skills/fastapi-best-practices/references/structure.md#fapi-001) | One `APIRouter` per domain, collected in `routers/`. |
| [`FAPI-002`](skills/fastapi-best-practices/references/structure.md#fapi-002) | Declare shared auth/validation dependencies on the `APIRouter`, not per route. |
| [`FAPI-003`](skills/fastapi-best-practices/references/structure.md#fapi-003) | Set `prefix`/`tags` at `include_router()` for routers reused across apps. |
| [`FAPI-004`](skills/fastapi-best-practices/references/structure.md#fapi-004) | Use a `lifespan` context manager for startup/shutdown; never `@app.on_event`. |
| [`FAPI-005`](skills/fastapi-best-practices/references/structure.md#fapi-005) | Share startup-created resources via `app.state`, not module-level globals. |
| [`FAPI-006`](skills/fastapi-best-practices/references/structure.md#fapi-006) | Version the API by URL path prefix (`/v1`), not by header. |
| [`FAPI-010`](skills/fastapi-best-practices/references/models-validation.md#fapi-010) | Configure Pydantic v2 models with `model_config = ConfigDict(...)`, not inner `class Config`. |
| [`FAPI-011`](skills/fastapi-best-practices/references/models-validation.md#fapi-011) | Use separate input and output models so secrets never leak into responses. |
| [`FAPI-012`](skills/fastapi-best-practices/references/models-validation.md#fapi-012) | Prefer a return-type annotation over `response_model=` when the shapes match. |
| [`FAPI-013`](skills/fastapi-best-practices/references/models-validation.md#fapi-013) | Set `response_model_exclude_unset=True` for sparse/partial-record responses. |
| [`FAPI-014`](skills/fastapi-best-practices/references/models-validation.md#fapi-014) | Pair `response_class=` with `response_model=None` when returning a `Response` directly. |
| [`FAPI-020`](skills/fastapi-best-practices/references/settings.md#fapi-020) | Use `pydantic-settings` `BaseSettings` for config, not scattered `os.getenv`. |
| [`FAPI-021`](skills/fastapi-best-practices/references/settings.md#fapi-021) | Wrap `Settings()` in `@lru_cache` and inject it via `Depends`. |
| [`FAPI-022`](skills/fastapi-best-practices/references/settings.md#fapi-022) | Type secret fields as `SecretStr`. |
| [`API-002`](skills/fastapi-best-practices/references/settings.md#api-002) | Rank mounted secret files above environment variables via `settings_customise_sources`. |
| [`FAPI-030`](skills/fastapi-best-practices/references/dependencies.md#fapi-030) | Declare dependencies as `Annotated[T, Depends(fn)]` and reuse via type aliases. |
| [`FAPI-031`](skills/fastapi-best-practices/references/dependencies.md#fapi-031) | Wrap `yield` dependencies in `try/finally` so cleanup runs on error. |
| [`FAPI-032`](skills/fastapi-best-practices/references/dependencies.md#fapi-032) | Use `Security(fn, scopes=[...])` (not `Depends`) for OAuth2 scope-bearing auth. |
| [`FAPI-040`](skills/fastapi-best-practices/references/path-operations.md#fapi-040) | Use `async def` for I/O-bound routes; reserve `def` for sync/CPU-bound work. |
| [`FAPI-041`](skills/fastapi-best-practices/references/path-operations.md#fapi-041) | Use `status.HTTP_*` constants and semantic codes (`201` create, `204` delete). |
| [`FAPI-042`](skills/fastapi-best-practices/references/path-operations.md#fapi-042) | Use cursor (keyset) pagination for large collections; offset only for small bounded sets. |
| [`FAPI-043`](skills/fastapi-best-practices/references/path-operations.md#fapi-043) | Use `BackgroundTasks` only for fire-and-forget work; escalate durable jobs to a queue. |
| [`FAPI-050`](skills/fastapi-best-practices/references/errors.md#fapi-050) | Register handlers against `StarletteHTTPException` to also catch routing 404/405. |
| [`FAPI-051`](skills/fastapi-best-practices/references/errors.md#fapi-051) | Override `RequestValidationError` to stabilize the 422 response shape. |
| [`FAPI-052`](skills/fastapi-best-practices/references/errors.md#fapi-052) | Register a catch-all `Exception` handler for a uniform `500` body; don't log in it (the server already does). |
| [`FAPI-053`](skills/fastapi-best-practices/references/errors.md#fapi-053) | Use a structured `detail` (dict with a code) for machine-readable errors. |
| [`FAPI-060`](skills/fastapi-best-practices/references/security.md#fapi-060) | Hash passwords with Argon2 via `pwdlib`; never plaintext, MD5, or SHA-1. |
| [`FAPI-061`](skills/fastapi-best-practices/references/security.md#fapi-061) | Verify against a dummy hash when the user lookup misses (timing attack). |
| [`FAPI-062`](skills/fastapi-best-practices/references/security.md#fapi-062) | Never put secrets, PII, or session state in a JWT payload. |
| [`FAPI-063`](skills/fastapi-best-practices/references/security.md#fapi-063) | Point `OAuth2PasswordBearer(tokenUrl=...)` at the real token endpoint path. |
| [`API-001`](skills/fastapi-best-practices/references/security.md#api-001) | Compare bearer tokens with `hmac.compare_digest` on bytes, never `==`. |
| [`FAPI-070`](skills/fastapi-best-practices/references/async-db.md#fapi-070) | Never use a sync SQLAlchemy `Session` inside an `async def` route. |
| [`FAPI-071`](skills/fastapi-best-practices/references/async-db.md#fapi-071) | Create one async engine per process, in `lifespan` — not per request. |
| [`FAPI-072`](skills/fastapi-best-practices/references/async-db.md#fapi-072) | Manage schema with Alembic; don't call `create_all()` in production. |
| [`FAPI-080`](skills/fastapi-best-practices/references/testing.md#fapi-080) | Use `TestClient` for sync tests, `httpx.AsyncClient` + `ASGITransport` for async. |
| [`FAPI-081`](skills/fastapi-best-practices/references/testing.md#fapi-081) | Swap dependencies with `app.dependency_overrides`, not monkeypatching. |
| [`FAPI-082`](skills/fastapi-best-practices/references/testing.md#fapi-082) | Enter `TestClient(app)` as a context manager so `lifespan` runs in tests. |
| [`FAPI-090`](skills/fastapi-best-practices/references/deployment.md#fapi-090) | Run one Uvicorn process per container; let the orchestrator replicate. |
| [`FAPI-091`](skills/fastapi-best-practices/references/deployment.md#fapi-091) | Set `--forwarded-allow-ips` and `root_path` when running behind a proxy. |
| [`FAPI-092`](skills/fastapi-best-practices/references/deployment.md#fapi-092) | Never combine `allow_origins=["*"]` with `allow_credentials=True` in CORS. |
| [`OBS-001`](skills/fastapi-best-practices/references/observability.md#obs-001) | `/healthz` is liveness (no dependency checks); `/readyz` is readiness (checks the database, 503 problem+json). |
| [`OBS-003`](skills/fastapi-best-practices/references/observability.md#obs-003) | Always instrument against the OTel API; start the SDK/exporters only when `OTEL_EXPORTER_OTLP_ENDPOINT` is set. |
| [`OBS-004`](skills/fastapi-best-practices/references/observability.md#obs-004) | Reuse a safe `X-Request-ID` (else generate one), echo it, and stamp it on every log line with `trace_id`/`span_id`. |
| [`OBS-005`](skills/fastapi-best-practices/references/observability.md#obs-005) | Push HTTP metrics over OTLP on the stable `http.server.request.duration` (`OTEL_SEMCONV_STABILITY_OPT_IN=http`); `OTEL_METRICS_EXPORTER=none` turns them off. |
| [`OBS-006`](skills/fastapi-best-practices/references/observability.md#obs-006) | Use pure ASGI middleware, not `BaseHTTPMiddleware`, for anything that sets a ContextVar. |

## go-best-practices

_Use when working with Go code — .go files and go.mod/go.sum, go build / go test / go test -race / go vet / gofmt / staticcheck / golangci-lint / govulncheck, module and package layout, error handling (errors.Is / %w), the context package, goroutines and channels, errgroup, stdlib net/http servers and clients, graceful shutdown on SIGINT/SIGTERM, parameterized SQL, or Go security (html/template, crypto/rand). Covers the GO- rule family (layout, errors, concurrency, HTTP, security, modules, quality, testing). Language-level Go only — container packaging lives in containers-best-practices, gRPC service rules in grpc-best-practices._

| ID | Summary |
|---|---|
| [`GO-001`](skills/go-best-practices/references/layout.md#go-001) | One Go module per repo; `go.mod` at the repo root, module path = canonical import path; keep `main()` a thin wrapper over a testable `run() error`. |
| [`GO-002`](skills/go-best-practices/references/layout.md#go-002) | Put private implementation under `internal/`; only public API lives above it. |
| [`GO-003`](skills/go-best-practices/references/layout.md#go-003) | Name the package after its directory; no `util`/`common`/`helpers` grab-bags. |
| [`GO-004`](skills/go-best-practices/references/errors.md#go-004) | Wrap errors with `%w` to preserve the cause for `errors.Is`/`errors.As`. |
| [`GO-005`](skills/go-best-practices/references/errors.md#go-005) | Match with `errors.Is` / `errors.As`, never `==`. |
| [`GO-006`](skills/go-best-practices/references/errors.md#go-006) | Return errors, don't `panic`/`os.Exit` in reusable code; recover only at a process boundary. |
| [`GO-007`](skills/go-best-practices/references/concurrency.md#go-007) | Pass `context.Context` as the first parameter of blocking/I-O functions; never store it in a struct field. |
| [`GO-008`](skills/go-best-practices/references/concurrency.md#go-008) | Bound every goroutine's lifetime with a stop path (WaitGroup, errgroup, cancelable ctx) — a goroutine without one is a leak. |
| [`GO-009`](skills/go-best-practices/references/concurrency.md#go-009) | Use `golang.org/x/sync/errgroup` for fan-out that cancels on first error. |
| [`GO-010`](skills/go-best-practices/references/http.md#go-010) | Set `ReadHeaderTimeout`/`ReadTimeout`/`WriteTimeout`/`IdleTimeout` on `http.Server`; wrap slow handlers in `http.TimeoutHandler`. |
| [`GO-011`](skills/go-best-practices/references/http.md#go-011) | Outbound HTTP: carry context and give the client a timeout; never lean on `http.DefaultClient`'s zero timeout. |
| [`GO-020`](skills/go-best-practices/references/http.md#go-020) | Shut down `http.Server` gracefully on SIGINT/SIGTERM via `signal.NotifyContext` + `srv.Shutdown`; treat `http.ErrServerClosed` as the clean sentinel. |
| [`GO-012`](skills/go-best-practices/references/security.md#go-012) | Never build SQL by concatenation or `fmt.Sprintf` — use `database/sql` placeholders. |
| [`GO-013`](skills/go-best-practices/references/security.md#go-013) | Escape untrusted HTML with `html/template`, never `text/template`. |
| [`GO-014`](skills/go-best-practices/references/security.md#go-014) | Use `crypto/rand` for secrets, tokens, and password-material; `math/rand` is not a security RNG. |
| [`GO-015`](skills/go-best-practices/references/modules.md#go-015) | Commit `go.mod` and `go.sum`; make all changes via `go` commands, not hand-editing; run `go mod tidy`. |
| [`GO-016`](skills/go-best-practices/references/quality.md#go-016) | Enforce `gofmt` + `go vet` over all packages in pre-commit and CI; add `staticcheck` or `golangci-lint` for deeper checks. |
| [`GO-019`](skills/go-best-practices/references/quality.md#go-019) | Scan dependencies and stdlib for known vulnerabilities in CI with `govulncheck` over all packages; call-graph reachability keeps the findings actionable. |
| [`GO-017`](skills/go-best-practices/references/testing.md#go-017) | Table-driven tests with subtests; `t.Helper()` in test helpers; `t.Parallel()` only for truly independent cases. |
| [`GO-018`](skills/go-best-practices/references/testing.md#go-018) | Gate CI on `go test -race` over all packages; the race detector is the only tool that finds data races, and a green run is evidence, not proof. |

## grpc-best-practices

_Use when working with gRPC — .proto files and proto3 schema design, enum zero values and _UNSPECIFIED sentinels, well-known types (google.protobuf.Timestamp / Duration / FieldMask), List pagination (page_size / page_token / next_page_token), buf or protoc generation, grpc-go / grpclib / grpcio servers and clients, google.rpc status codes and rich errors, deadlines and cancellation propagation, streaming RPCs (server/client/bidi), interceptors (auth, logging, panic recovery, protovalidate), TLS / mTLS transport credentials and the insecure-credentials footgun, server reflection + grpcurl/grpcui, health checking, message-size limits and connection reuse. Covers the GRPC- rule family (proto design, error model, deadlines, streaming, interceptors, security, tooling, performance). Protocol-level rules apply to any language; code examples are grpc-go. For the language underneath, see go-best-practices._

| ID | Summary |
|---|---|
| [`GRPC-001`](skills/grpc-best-practices/references/proto-design.md#grpc-001) | Field numbers are permanent: evolve proto3 additively; never renumber, repurpose, or change a field's type. |
| [`GRPC-002`](skills/grpc-best-practices/references/proto-design.md#grpc-002) | Distinguish "unset" from zero with proto3 `optional` (or wrappers); proto3 has no `required`. |
| [`GRPC-003`](skills/grpc-best-practices/references/proto-design.md#grpc-003) | Dedicated `XxxRequest`/`XxxResponse` pair per RPC; messages are nouns, RPCs are verb+noun. |
| [`GRPC-019`](skills/grpc-best-practices/references/proto-design.md#grpc-019) | Reserve enum `0` for a `_UNSPECIFIED` sentinel; never put a real value there. |
| [`GRPC-020`](skills/grpc-best-practices/references/proto-design.md#grpc-020) | Model time with `google.protobuf.Timestamp`/`Duration`, not raw `int64` or strings. |
| [`GRPC-021`](skills/grpc-best-practices/references/proto-design.md#grpc-021) | Paginate unbounded `List` RPCs with `page_size`/`page_token`/`next_page_token`. |
| [`GRPC-004`](skills/grpc-best-practices/references/error-model.md#grpc-004) | Return canonical `google.rpc.Code` values mapped from your domain; never invent codes or leak HTTP/transport assumptions. |
| [`GRPC-005`](skills/grpc-best-practices/references/error-model.md#grpc-005) | Add machine-readable detail (`google.rpc.ErrorInfo` via `status.WithDetails`) so clients branch on data, not message strings. |
| [`GRPC-006`](skills/grpc-best-practices/references/error-model.md#grpc-006) | Translate internal errors to gRPC status only at the service boundary; log the cause, return generic `INTERNAL` for unknown failures. |
| [`GRPC-007`](skills/grpc-best-practices/references/deadlines.md#grpc-007) | Clients set a deadline on every RPC (`context.WithTimeout`); a deadline-free call can hang a goroutine and cascade. |
| [`GRPC-008`](skills/grpc-best-practices/references/deadlines.md#grpc-008) | Handlers honor the context: check `ctx.Err()`, propagate ctx downstream, return on the deadline budget instead of continuing zombie work. |
| [`GRPC-009`](skills/grpc-best-practices/references/streaming.md#grpc-009) | Choose unary vs streaming honestly: a `repeated` field in one message beats a stream of thousands of tiny messages. |
| [`GRPC-010`](skills/grpc-best-practices/references/streaming.md#grpc-010) | Server streams must stop on client cancel: bound work by `stream.Context()`, handle send errors, return on `Done`. |
| [`GRPC-011`](skills/grpc-best-practices/references/interceptors.md#grpc-011) | Cross-cutting concerns (auth, logging, recovery, validation) live in interceptors, composed with deliberate ordering. |
| [`GRPC-012`](skills/grpc-best-practices/references/interceptors.md#grpc-012) | Recover panics in a server interceptor (grpc-go has none built in) so one bad handler can't crash the process. |
| [`GRPC-013`](skills/grpc-best-practices/references/interceptors.md#grpc-013) | Validate with `buf.validate` + a protovalidate interceptor; return `INVALID_ARGUMENT`, not hand-rolled per-handler checks. |
| [`GRPC-018`](skills/grpc-best-practices/references/security.md#grpc-018) | Run production gRPC over TLS; `insecure.NewCredentials()` is dev-only. |
| [`GRPC-014`](skills/grpc-best-practices/references/tooling.md#grpc-014) | Manage schemas with `buf`; gate CI on `buf lint` + `buf breaking --against`; keep generated code in sync. |
| [`GRPC-015`](skills/grpc-best-practices/references/tooling.md#grpc-015) | Enable server reflection in dev for grpcurl/grpcui; wire health checking (`grpc.health.v1`) for production probes. |
| [`GRPC-016`](skills/grpc-best-practices/references/performance.md#grpc-016) | Reuse one client connection per backend process (HTTP/2 multiplexes); never dial per call; set keepalive. |
| [`GRPC-017`](skills/grpc-best-practices/references/performance.md#grpc-017) | Set `MaxRecvMsgSize`/`MaxSendMsgSize` deliberately; the 4 MiB default fails loudly — stream or batch instead of unbounded limits. |

## meta-best-practices

_Use when adding, editing, or reviewing rules in the best-practices library — covers the rule-ID format (PREFIX-NNN), severity scale, four-part What/Why/How/When-NOT-to-apply reference structure, SKILL.md shape, third-person description-field rules, and how to use tools/new-skill.sh + tools/new-rule.sh + tools/lint.sh + tools/render-index.sh. Activate on phrases like "best practices format", "add a rule", "rule format", "severity scale", "new domain skill", or when generating content for any *-best-practices skill._

_(no rules listed in SKILL.md rule index yet)_

## python-best-practices

_Use when working with Python projects — `.py` files, `pyproject.toml` (without `[tool.uv]`), `conftest.py`, `mypy.ini`, `pyrightconfig.json`, `.python-version` — or when the user asks about Python project layout, typing, linting, testing, async, logging, packaging, dependency auditing, or profiling. Mentions of pytest, mypy, pyright, basedpyright, ruff, structlog, hatch, or setuptools-scm are triggers. Covers the PY- rule family. For uv-specific patterns (`uv sync`, `[tool.uv]`, `uv.lock`) use uv-best-practices; for Python-in-Docker use containers-best-practices._

| ID | Summary |
|---|---|
| [`PY-002`](skills/python-best-practices/references/project-layout.md#py-002) | `pyproject.toml` is the single source of truth — no `setup.py`, no `setup.cfg`, no `MANIFEST.in` for metadata. |
| [`PY-003`](skills/python-best-practices/references/project-layout.md#py-003) | Required `[project]` metadata for publishing: `description`, `readme`, `requires-python`, `license` (SPDX), `classifiers`, `urls`. |
| [`PY-004`](skills/python-best-practices/references/project-layout.md#py-004) | Pick `[build-system]` deliberately — `hatchling` is the sensible default for pure Python; `uv_build` for uv-managed pure-Python projects (PY-089); `maturin` / `scikit-build-core` for native extensions. |
| [`PY-006`](skills/python-best-practices/references/project-layout.md#py-006) | `__init__.py`: explicit `__all__` for re-exports; no side effects, no I/O, no global mutation at import time. |
| [`PY-010`](skills/python-best-practices/references/typing.md#py-010) | Use modern syntax: `X \| Y` (PEP 604), `list[str]` / `dict[str, int]` (PEP 585), `Self` (PEP 673). No `Union[X, Y]`, no `typing.List`. |
| [`PY-011`](skills/python-best-practices/references/typing.md#py-011) | Default to `basedpyright` (or `pyright`) for new projects; stay on `mypy` for established codebases unless conformance gaps block you. `pyrefly` (Meta, stable 1.0) is a fast new option worth evaluating when you don't need mypy plugins; `ty` (Astral) is still beta. |
| [`PY-012`](skills/python-best-practices/references/typing.md#py-012) | Configure strict mode in `pyproject.toml` (`[tool.mypy] strict = true` or `[tool.pyright] typeCheckingMode = "strict"`). |
| [`PY-013`](skills/python-best-practices/references/typing.md#py-013) | `Protocol` for structural interfaces (duck typing with type safety); `abc.ABC` only when enforced inheritance is required. |
| [`PY-014`](skills/python-best-practices/references/typing.md#py-014) | Use `if TYPE_CHECKING:` for type-only imports + `from __future__ import annotations` at the top of files that need forward refs (on Python 3.14+ deferred evaluation is the default, so the future import is no longer needed for forward refs — see PY-080). |
| [`PY-015`](skills/python-best-practices/references/typing.md#py-015) | `TypedDict` for JSON / API shapes (no runtime cost); `@dataclass` for internal records; `pydantic.BaseModel` only at system boundaries that need runtime validation. |
| [`PY-016`](skills/python-best-practices/references/typing.md#py-016) | `# type: ignore[code]` and `# noqa: CODE` always with the explicit code — never bare. Bare ignores swallow future errors silently. |
| [`PY-069`](skills/python-best-practices/references/typing.md#py-069) | Prefer `TypeIs` (PEP 742) over `TypeGuard` for narrowing predicates — `TypeIs` narrows in both branches so `assert_never` exhaustiveness works. |
| [`PY-071`](skills/python-best-practices/references/typing.md#py-071) | Use PEP 695 inline generic syntax (`def foo[T]`, `class Foo[T]`, `type Alias = ...`) on Python 3.12+; PEP 696 defaults require 3.13. |
| [`PY-080`](skills/python-best-practices/references/typing.md#py-080) | On Python 3.14+, drop `from __future__ import annotations` — deferred annotation evaluation (PEP 649/749) is the default. |
| [`PY-081`](skills/python-best-practices/references/typing.md#py-081) | Use `TypedDict(closed=True)` / `extra_items=` for strict JSON shapes (PEP 728, Python 3.15+). |
| [`PY-082`](skills/python-best-practices/references/typing.md#py-082) | Use `TypeForm[T]` (PEP 747) for functions that accept a type expression at runtime, instead of `type[T]` or `Any`. |
| [`PY-020`](skills/python-best-practices/references/linting.md#py-020) | Use `ruff` for both linting and formatting; drop `black`, `isort`, `flake8`, `pyupgrade`. Keep `mypy` / `pyright` (ruff isn't a type checker). |
| [`PY-021`](skills/python-best-practices/references/linting.md#py-021) | Recommended ruff rule selection: `E, W, F, B, I, UP, S, SIM, RUF`. Set `target-version` so `UP` rules respect your Python floor. |
| [`PY-022`](skills/python-best-practices/references/linting.md#py-022) | Per-file ignores for tests (`S101` asserts OK) and `__init__.py` (`F401` re-exports OK) via `[tool.ruff.lint.per-file-ignores]`. |
| [`PY-023`](skills/python-best-practices/references/linting.md#py-023) | Line length: 88 (ruff/black default). Don't fight the default unless your team already has a different standard. |
| [`PY-024`](skills/python-best-practices/references/linting.md#py-024) | pre-commit hook order: `ruff` (with `--fix`) **before** `ruff-format`. Lint fixes can produce output that needs reformatting. |
| [`PY-025`](skills/python-best-practices/references/linting.md#py-025) | Enable `RUF100` to flag unused `noqa` directives so suppression comments don't accumulate as dead weight. |
| [`PY-088`](skills/python-best-practices/references/linting.md#py-088) | Use ruff range suppression (`# ruff: disable[CODE]` / `# ruff: enable[CODE]`, ≥0.15) for multi-line blocks; always name the code. |
| [`PY-091`](skills/python-best-practices/references/linting.md#py-091) | Never `return` / `break` / `continue` out of a `finally` block (ruff `B012`; PEP 765 makes it a `SyntaxWarning` on 3.14). |
| [`PY-030`](skills/python-best-practices/references/testing.md#py-030) | pytest config in `pyproject.toml [tool.pytest.ini_options]`; never `setup.cfg`. Set `markers`, `testpaths`, `addopts = "--strict-markers"`. On pytest 9+, prefer the native `[tool.pytest]` table and the `strict = true` mega-option. |
| [`PY-031`](skills/python-best-practices/references/testing.md#py-031) | Always `@pytest.mark.parametrize` over for-loops in test bodies — separate test IDs, isolated state, individually runnable. |
| [`PY-032`](skills/python-best-practices/references/testing.md#py-032) | Default fixtures to `function` scope; escalate to `session` only for genuinely expensive resources. Use `yield` for teardown. |
| [`PY-033`](skills/python-best-practices/references/testing.md#py-033) | Register every custom marker in `[tool.pytest.ini_options].markers`. Use `@pytest.mark.xfail(strict=True)` so unexpected passes fail loudly. |
| [`PY-034`](skills/python-best-practices/references/testing.md#py-034) | Async tests: pick `pytest-asyncio` *or* `anyio` plugin and configure one. Don't mix. Note `pytest-asyncio`'s default mode is `strict` (async tests need `@pytest.mark.asyncio` unless you set `asyncio_mode = "auto"`); the old `event_loop` fixture is removed in the 1.x line. |
| [`PY-035`](skills/python-best-practices/references/testing.md#py-035) | Coverage gate in `pyproject.toml [tool.coverage.report] fail_under = N` with `branch = true`. Treat 80% as a starting heuristic, not a magic number. |
| [`PY-074`](skills/python-best-practices/references/testing.md#py-074) | Add `pytest-randomly` to shuffle test order per run and detect order-dependent tests; pairs with function-scope fixtures (PY-032). |
| [`PY-086`](skills/python-best-practices/references/testing.md#py-086) | Enable ruff's `PT` family to mechanically enforce pytest style (bare `pytest.raises`, wrong `parametrize` shapes, unittest-style asserts). |
| [`PY-087`](skills/python-best-practices/references/testing.md#py-087) | Add `pytest-xdist` (`-n auto`) for parallel runs once the suite passes reliably under random order. |
| [`PY-040`](skills/python-best-practices/references/async.md#py-040) | `asyncio` for I/O-bound concurrency only; CPU-bound work goes to `ProcessPoolExecutor` via `loop.run_in_executor`. |
| [`PY-041`](skills/python-best-practices/references/async.md#py-041) | Never call blocking I/O inside async (`requests`, `time.sleep`, sync DB drivers); enable `asyncio.run(..., debug=True)` / `PYTHONASYNCIODEBUG=1` in dev to detect. |
| [`PY-042`](skills/python-best-practices/references/async.md#py-042) | `asyncio.run()` at the top level only; never nest. `nest_asyncio` is an antipattern outside Jupyter notebooks. |
| [`PY-043`](skills/python-best-practices/references/async.md#py-043) | Prefer `asyncio.TaskGroup` (3.11+) over `asyncio.gather` for structured concurrency; always re-raise `CancelledError` after cleanup. |
| [`PY-044`](skills/python-best-practices/references/async.md#py-044) | Wrap async generators in `contextlib.aclosing()` when iteration may break early — don't rely on GC to call `aclose()`. |
| [`PY-045`](skills/python-best-practices/references/async.md#py-045) | Library code that must work on asyncio *and* Trio uses `anyio`; applications that own their runtime use `asyncio` directly. |
| [`PY-067`](skills/python-best-practices/references/async.md#py-067) | Keep a strong reference to fire-and-forget `asyncio.create_task` results; tasks held only weakly can be GC'd mid-execution. |
| [`PY-068`](skills/python-best-practices/references/async.md#py-068) | Prefer `async with asyncio.timeout(N):` (3.11+) over `asyncio.wait_for` for block-level timeouts; reserve `wait_for` for wrapping a single coroutine. |
| [`PY-079`](skills/python-best-practices/references/async.md#py-079) | Free-threaded Python (PEP 703 / PEP 779) — informational guard-rail: not production-ready until your entire dep graph declares free-threading support. (3.14 reduced single-thread overhead to ~5–10%.) |
| [`PY-083`](skills/python-best-practices/references/async.md#py-083) | On Python 3.14+, use `concurrent.interpreters.InterpreterPoolExecutor` for CPU-bound work needing process-like isolation at thread-like cost. |
| [`PY-084`](skills/python-best-practices/references/async.md#py-084) | Use `asyncio.get_running_loop()`, never `asyncio.get_event_loop()` — the latter raises `RuntimeError` on 3.14+ with no running loop. |
| [`PY-085`](skills/python-best-practices/references/async.md#py-085) | Introspect stuck async apps with `python -m asyncio ps <pid>` / `pstree <pid>` (3.14+) instead of adding instrumentation. |
| [`PY-050`](skills/python-best-practices/references/logging.md#py-050) | Module-level `logger = logging.getLogger(__name__)` at the top of every file. Never call `logging.info(...)` (root logger) directly. |
| [`PY-051`](skills/python-best-practices/references/logging.md#py-051) | Pass positional args to log methods (`logger.info("user %s", user_id)`); never f-strings or `.format()`. Defers interpolation; preserves Sentry / aggregator grouping. |
| [`PY-052`](skills/python-best-practices/references/logging.md#py-052) | Libraries add only `logging.NullHandler()` and configure nothing else. Never call `basicConfig`, never set levels, never add handlers in library code. |
| [`PY-053`](skills/python-best-practices/references/logging.md#py-053) | Applications configure via `logging.config.dictConfig(...)` with `disable_existing_loggers: False`. Never use `basicConfig` outside one-off scripts. |
| [`PY-054`](skills/python-best-practices/references/logging.md#py-054) | `logger.exception("msg")` inside `except` blocks; `logger.error("msg", exc_info=True)` everywhere else. Outside `except`, `exception()` logs `NoneType: None`. |
| [`PY-055`](skills/python-best-practices/references/logging.md#py-055) | Propagate context (`request_id`, `user_id`) via `contextvars` + a `logging.Filter` (or `structlog.contextvars`). Redact secrets at the formatter/filter, not call sites. |
| [`PY-056`](skills/python-best-practices/references/logging.md#py-056) | Production: JSON to stdout, no in-process rotation — let the container/systemd handle log capture and rotation. Application code uses `structlog`; library code uses stdlib `logging`. |
| [`PY-076`](skills/python-best-practices/references/logging.md#py-076) | Wrap downstream handlers in `QueueHandler` / `QueueListener` so log I/O happens off the request thread / event loop. 3.12+ resolves handlers by name in `dictConfig`. |
| [`PY-060`](skills/python-best-practices/references/packaging.md#py-060) | `license` is an SPDX string (PEP 639): `license = "MIT"`. The `{text = "MIT"}` / `{file = "LICENSE"}` table forms are deprecated. |
| [`PY-061`](skills/python-best-practices/references/packaging.md#py-061) | Derive version from git tags via `hatch-vcs` (hatchling) or `setuptools-scm` (setuptools); declare `dynamic = ["version"]`. Don't hand-maintain a version in both `pyproject.toml` and `__init__.py`. |
| [`PY-062`](skills/python-best-practices/references/packaging.md#py-062) | Publish from CI via **Trusted Publishing (OIDC)**, not API tokens. Use `pypa/gh-action-pypi-publish` with `id-token: write` permission. |
| [`PY-063`](skills/python-best-practices/references/packaging.md#py-063) | Build with `uv build` or `python -m build` — both produce sdist + wheel together. Verify with `twine check dist/*` before upload. |
| [`PY-064`](skills/python-best-practices/references/packaging.md#py-064) | Upload to TestPyPI first to catch metadata / README rendering issues. PyPI doesn't allow overwriting a release. |
| [`PY-065`](skills/python-best-practices/references/packaging.md#py-065) | Dev dependencies go in `[project.optional-dependencies]` (or `[dependency-groups]` per UVP-001), never `[project.dependencies]`. End-users of your library shouldn't install pytest. |
| [`PY-066`](skills/python-best-practices/references/packaging.md#py-066) | `warnings.warn(..., DeprecationWarning, stacklevel=2)` for public API deprecations. `stacklevel=2` points the warning at the caller, not your library's internals. |
| [`PY-070`](skills/python-best-practices/references/packaging.md#py-070) | `@warnings.deprecated` (PEP 702, 3.13+ / `typing_extensions`) for type-checker-visible deprecations — flags call sites inline at edit time, not just at runtime. |
| [`PY-072`](skills/python-best-practices/references/packaging.md#py-072) | Use PEP 723 inline script metadata (`# /// script` block) for standalone scripts with deps; `uv run script.py` consumes it directly. |
| [`PY-073`](skills/python-best-practices/references/packaging.md#py-073) | `datetime.now(UTC)` / always-aware datetimes; `datetime.utcnow()` and `datetime.utcfromtimestamp()` are deprecated as of 3.12. Pair with ruff `DTZ` (opt-in per PY-021). |
| [`PY-075`](skills/python-best-practices/references/packaging.md#py-075) | Enable PEP 740 build provenance attestations — automatic in `pypa/gh-action-pypi-publish` v1.11+ with `permissions: attestations: write`. Extends PY-062. |
| [`PY-089`](skills/python-best-practices/references/packaging.md#py-089) | For uv-managed pure-Python projects, prefer the `uv_build` backend (zero-config, fast); see UVP-029 for the uv-side detail. Stay on hatchling for build hooks or non-uv workflows. |
| [`PY-077`](skills/python-best-practices/references/packaging.md#py-077) | Audit dependencies for known CVEs in CI with `pip-audit` (PyPA, OSV-backed) or `uv audit`. Don't gate releases on it silently — fail the build. |
| [`PY-090`](skills/python-best-practices/references/packaging.md#py-090) | Pin dependency hashes (`uv lock`, or `--generate-hashes` for pip) and verify them with `--frozen` / `--require-hashes` in CI to blunt substitution attacks. |
| [`PY-078`](skills/python-best-practices/references/packaging.md#py-078) | Profile with `scalene` (local, line-level CPU + memory + native time) or `py-spy` (production sampling, attach by PID) *before* reaching for `mypyc`, Cython, or Rust extensions. |

## repo-best-practices

_Use when editing repository hygiene and CI config — .pre-commit-config.yaml hooks, .github/dependabot.yml, GitHub Actions workflows (.github/workflows/*.yml, permissions, zizmor audits), CODEOWNERS, justfile task-runner recipes that CI calls, or secret scanning (gitleaks). Covers the REPO- rule family (pre-commit as a maintained artifact, one dependency bot with a cooldown, secret scanning, justfile/CI parity, CODEOWNERS, workflow static analysis, least-privilege GITHUB_TOKEN). Language-agnostic repo plumbing; language rules live in the language skills._

| ID | Summary |
|---|---|
| [`REPO-001`](skills/repo-best-practices/references/hooks-and-bots.md#repo-001) | Treat `.pre-commit-config.yaml` as a first-class artifact: SHA-pinned remote hooks, local hooks running the project's own tools, a why-comment on every hook. |
| [`REPO-002`](skills/repo-best-practices/references/hooks-and-bots.md#repo-002) | One dependency bot (Dependabot) maintains every pin — lockfile, actions, pre-commit revs, dev container Features — with a cooldown and no auto-merge. |
| [`REPO-004`](skills/repo-best-practices/references/hooks-and-bots.md#repo-004) | Secret-scan the repo itself (gitleaks, detect-secrets or trufflehog) on every commit. |
| [`REPO-007`](skills/repo-best-practices/references/hooks-and-bots.md#repo-007) | Every CI step is a `just` recipe, `just check` runs them all, and a test enforces the parity and `just --list` help text. |
| [`REPO-008`](skills/repo-best-practices/references/hooks-and-bots.md#repo-008) | Keep a `CODEOWNERS` file for review routing. |
| [`REPO-009`](skills/repo-best-practices/references/github-actions.md#repo-009) | Statically audit workflows with zizmor (injection, over-broad permissions, persisted credentials, unpinned actions). |
| [`REPO-010`](skills/repo-best-practices/references/github-actions.md#repo-010) | Start workflows from `permissions: {}` and grant each job only the `GITHUB_TOKEN` scopes it needs. |

## uv-best-practices

_Use when working with uv (Astral's Python package and project manager) — `uv sync` / `add` / `lock` / `run` / `tool` / `build` / `publish` / `audit` / `export`, `uvx`, a `uv.lock` or `pylock.toml` file, or a `pyproject.toml` with `[tool.uv]` or `[dependency-groups]`. Covers project shape, lockfile hygiene, workspaces, dependency groups, private registries, Python version pinning, CI, publishing, vulnerability scanning, and migration from pip / poetry / PDM / Hatch. Covers the UVP- rule family. For container-side uv patterns (Dockerfile builds) use containers-best-practices instead._

| ID | Summary |
|---|---|
| [`UVP-001`](skills/uv-best-practices/references/project-shape.md#uvp-001) | Use `[dependency-groups]` (PEP 735), not deprecated `[tool.uv.dev-dependencies]`. |
| [`UVP-002`](skills/uv-best-practices/references/project-shape.md#uvp-002) | Compose groups with `include-group`; never duplicate packages across groups. |
| [`UVP-003`](skills/uv-best-practices/references/project-shape.md#uvp-003) | `requires-python` is a *floor* (`">=3.X"`), never an upper-bounded range. |
| [`UVP-004`](skills/uv-best-practices/references/project-shape.md#uvp-004) | Git and path dependencies go in `[tool.uv.sources]`, not as raw URLs in `[project.dependencies]`. |
| [`UVP-005`](skills/uv-best-practices/references/project-shape.md#uvp-005) | `uv init --lib` for libraries (src/ + build-system); bare `uv init` for apps (flat, `package = false`). |
| [`UVP-006`](skills/uv-best-practices/references/project-shape.md#uvp-006) | Use a workspace only when members share lockfile resolution; conflicting deps or different Python floors mean you don't want one. |
| [`UVP-007`](skills/uv-best-practices/references/project-shape.md#uvp-007) | Workspace inter-member deps need *both* `[project.dependencies]` and `[tool.uv.sources]` with `workspace = true`. |
| [`UVP-008`](skills/uv-best-practices/references/project-shape.md#uvp-008) | Pin a minimum uv version with `[tool.uv] required-version` (≥0.5.14) so all contributors and CI agree on resolver behavior. |
| [`UVP-016`](skills/uv-best-practices/references/project-shape.md#uvp-016) | Declare mutually-exclusive extras / groups (cpu vs gpu, redis vs memcached) with `[tool.uv] conflicts` (≥0.5.3) so uv forks resolution instead of producing a broken universal lock. |
| [`UVP-063`](skills/uv-best-practices/references/project-shape.md#uvp-063) | Set `[tool.uv] add-bounds` (stable ≥0.10.0) to control how `uv add` writes version constraints. |
| [`UVP-064`](skills/uv-best-practices/references/project-shape.md#uvp-064) | Bound lockfile platform scope with `environments` (restrict) or `required-environments` (guarantee wheels). |
| [`UVP-010`](skills/uv-best-practices/references/lockfile.md#uvp-010) | Commit `uv.lock` for **both** apps and libraries. |
| [`UVP-011`](skills/uv-best-practices/references/lockfile.md#uvp-011) | `--locked` validates lock vs `pyproject.toml`; `--frozen` skips validation **and** skips installing the project package — they're not interchangeable. |
| [`UVP-012`](skills/uv-best-practices/references/lockfile.md#uvp-012) | Use targeted `uv lock --upgrade-package <name>` over blanket `uv lock --upgrade` in production pipelines. |
| [`UVP-013`](skills/uv-best-practices/references/lockfile.md#uvp-013) | Never manually edit `uv.lock` — it's fully generated and uv will silently regenerate or error on tampered files. |
| [`UVP-014`](skills/uv-best-practices/references/lockfile.md#uvp-014) | Don't commit `requirements.txt` alongside `uv.lock`; if downstream tooling needs it, generate as a build artifact via `uv export`. |
| [`UVP-015`](skills/uv-best-practices/references/lockfile.md#uvp-015) | `uv lock --check` (lockfile vs `pyproject.toml`) and `uv sync --check` (env vs lockfile, ≥0.6.10) are composable, not interchangeable — use both at the right gates. |
| [`UVP-070`](skills/uv-best-practices/references/lockfile.md#uvp-070) | `uv lock --upgrade-group <group>` (≥0.11.4) refreshes one dependency group without touching others. |
| [`UVP-020`](skills/uv-best-practices/references/environments.md#uvp-020) | Production sync incantation: `uv sync --locked --no-dev --no-editable`. |
| [`UVP-021`](skills/uv-best-practices/references/environments.md#uvp-021) | `uv run` over manual `.venv/bin/activate` — it re-checks the lockfile before executing. |
| [`UVP-022`](skills/uv-best-practices/references/environments.md#uvp-022) | Set `UV_PROJECT_ENVIRONMENT` only to **absolute** paths; relative paths in multi-project repos silently target shared/system environments. |
| [`UVP-023`](skills/uv-best-practices/references/environments.md#uvp-023) | Configure `default-groups` deliberately; the default `["dev"]` means bare `uv sync` installs dev dependencies in production. |
| [`UVP-024`](skills/uv-best-practices/references/environments.md#uvp-024) | PEP 723 inline scripts: manage deps via `uv add --script`, lock with `uv lock --script` — and remember `uv run --script` runs isolated from any surrounding project's lockfile. |
| [`UVP-025`](skills/uv-best-practices/references/environments.md#uvp-025) | Configure additional indexes via `[[tool.uv.index]]` with `explicit = true` for private registries; never use `index-strategy = "unsafe-best-match"` (re-introduces dependency-confusion risk). |
| [`UVP-066`](skills/uv-best-practices/references/environments.md#uvp-066) | `uv run --with` builds an ephemeral overlay env (≥0.8.0); add `--isolated`/`uvx --isolated` when project packages must not leak in. |
| [`UVP-068`](skills/uv-best-practices/references/environments.md#uvp-068) | `uv run/sync --active` (or `UV_ACTIVE=1`) makes uv use the *activated* venv instead of silently creating `.venv`. |
| [`UVP-028`](skills/uv-best-practices/references/ci.md#uvp-028) | Run `uv audit` in CI to scan `uv.lock` against the OSV vulnerability database; exits non-zero on findings. |
| [`UVP-030`](skills/uv-best-practices/references/ci.md#uvp-030) | Pin `astral-sh/setup-uv` to a commit SHA with the version tag in a comment. |
| [`UVP-031`](skills/uv-best-practices/references/ci.md#uvp-031) | Let `setup-uv` handle caching (`enable-cache: true`); never cache `.venv/` across runs — Python ABI / OS drift will silently break it. |
| [`UVP-032`](skills/uv-best-practices/references/ci.md#uvp-032) | `uv sync --locked` in CI; bare `uv sync` silently re-resolves on a stale lockfile and ships untested versions. |
| [`UVP-033`](skills/uv-best-practices/references/ci.md#uvp-033) | Set `UV_LINK_MODE=copy` in CI systems with cross-filesystem caches (GitLab, some Docker layered builds) to silence hardlink warnings and avoid mode confusion. |
| [`UVP-065`](skills/uv-best-practices/references/ci.md#uvp-065) | Run `uv audit` with `--output-format sarif` (≥0.11.22) and centralize accepted-risk ignores in `[tool.uv.audit]`, not inline `--ignore` flags. |
| [`UVP-069`](skills/uv-best-practices/references/ci.md#uvp-069) | In monorepo CI, enumerate members with `uv workspace list` (stable ≥0.10.0) instead of hardcoding paths. |
| [`UVP-040`](skills/uv-best-practices/references/tools.md#uvp-040) | Tools in *this* project's workflow (`pytest`, `mypy`, `ruff`, `pre-commit`) → `[dependency-groups]`, locked. Personal cross-project CLIs (`cookiecutter`, `httpie`) → `uv tool install`. |
| [`UVP-041`](skills/uv-best-practices/references/tools.md#uvp-041) | `uvx` (= `uv tool run`) for one-shot tool execution; pin the version when correctness matters (`uvx ruff@0.6.9`, not bare `uvx ruff`). |
| [`UVP-042`](skills/uv-best-practices/references/tools.md#uvp-042) | `uv tool upgrade --all` is fine locally but unsafe in CI — silent version drift; declare tools in `[dependency-groups]` or pin via `uvx <tool>@<version>` instead. |
| [`UVP-072`](skills/uv-best-practices/references/tools.md#uvp-072) | Audit global tools with `uv tool list --outdated` (≥0.10.10) and `--show-python` before a Python upgrade. |
| [`UVP-050`](skills/uv-best-practices/references/python-versions.md#uvp-050) | `.python-version` pins the runtime; `requires-python` declares the compat floor. They look similar and aren't interchangeable. |
| [`UVP-051`](skills/uv-best-practices/references/python-versions.md#uvp-051) | Pin the *minor* version in `.python-version` (`3.12`), not the patch (`3.12.7`) — patch upgrades are transparent; minor changes affect resolution. |
| [`UVP-052`](skills/uv-best-practices/references/python-versions.md#uvp-052) | `UV_PYTHON_DOWNLOADS=never` in network-restricted CI — surfaces "Python not found" instead of a cryptic network error. |
| [`UVP-053`](skills/uv-best-practices/references/python-versions.md#uvp-053) | `uv version` (≥0.7.0) manages the *project* version, not uv's own; outside a project it is a **hard error** since 0.8.0. Use `uv self version` for the binary. |
| [`UVP-054`](skills/uv-best-practices/references/python-versions.md#uvp-054) | For libraries, matrix CI on `--resolution lowest-direct` and `--resolution highest` to catch implicit floor violations and upper-bound rot. |
| [`UVP-067`](skills/uv-best-practices/references/python-versions.md#uvp-067) | Keep managed interpreters patched with `uv python upgrade` (stable ≥0.10.0); it auto-upgrades venvs on the old patch. |
| [`UVP-073`](skills/uv-best-practices/references/python-versions.md#uvp-073) | uv's default managed Python is **3.14** since 0.9.0 — pin explicitly with `uv python pin` rather than relying on the default. |
| [`UVP-026`](skills/uv-best-practices/references/packaging.md#uvp-026) | Pre-publish sanity check: `uv build --no-sources` to verify the wheel doesn't accidentally depend on `[tool.uv.sources]` config that doesn't ship. |
| [`UVP-027`](skills/uv-best-practices/references/packaging.md#uvp-027) | Publish from GitHub Actions via PyPI OIDC trusted publishing (stable since uv 0.6.0) — no `UV_PUBLISH_TOKEN`, auto-uploaded PEP 740 attestations. |
| [`UVP-029`](skills/uv-best-practices/references/packaging.md#uvp-029) | Use the native `uv_build` PEP 517 backend (the **default** for `uv init --lib`/`--package` since uv 0.8.0) for pure-Python packages — 10–35x faster than hatchling / setuptools. |
| [`UVP-071`](skills/uv-best-practices/references/packaging.md#uvp-071) | `uv_build` supports PEP 794 `import-names` (≥0.11.17) for packages whose import name differs from the distribution name. |
| [`UVP-060`](skills/uv-best-practices/references/migration.md#uvp-060) | Use `astral-sh/migrate-to-uv` (or `uvx migrate-to-uv`) for poetry / pip-tools / PDM / Hatch projects; *manually verify* the resolved versions diff — resolver drift between Poetry and uv has shipped wrong packages to production. |
| [`UVP-061`](skills/uv-best-practices/references/migration.md#uvp-061) | `uv export --format requirements.txt --no-hashes --no-dev -o requirements.txt` for downstream consumers (Lambda layers, deploy targets that need it) — generate in CI, don't commit. |
| [`UVP-062`](skills/uv-best-practices/references/migration.md#uvp-062) | Prefer `uv export --format pylock.toml` (PEP 751, ≥0.6.15) over `requirements.txt` when the downstream tool supports it; never replace `uv.lock` with `pylock.toml` (the format can't represent uv's full graph). |
| [`UVP-074`](skills/uv-best-practices/references/migration.md#uvp-074) | Generate a CycloneDX SBOM with `uv export --format cyclonedx` (≥0.9.11) for supply-chain compliance, separate from the lockfile. |

